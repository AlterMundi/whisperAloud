# Core dependencies for WhisperAloud
# Alternative to: pip install -e .
# Preferred method: pip install -e . (uses pyproject.toml)

faster-whisper>=1.1.0
numpy>=1.24.0
sounddevice>=0.4.6
soundfile>=0.12.0
scipy>=1.11.0
psutil>=5.9.0
pydbus>=0.6.0
